name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: make
      run: make
    - name: make run
      run: make run
